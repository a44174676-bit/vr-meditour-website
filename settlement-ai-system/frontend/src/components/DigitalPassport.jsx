import React from "react";
export default function DigitalPassport({ passport }) { if(!passport) return null; return <div className='card'><h3>{passport.user_name} - Digital Passport</h3><p>정착단계: {passport.current_stage}</p><p>진행률: {passport.progress}%</p><p>완료 {passport.completed_tasks}/{passport.total_tasks}</p><p>위험도: {passport.risk_level} (정착 {passport.settlement_risk_score}, 고립 {passport.social_isolation_score})</p></div>; }
